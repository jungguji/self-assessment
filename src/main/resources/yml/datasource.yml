spring:
  datasource:
    url: jdbc:mysql://localhost:3306/salf_assessment?serverTimezone=UTC&characterEncoding=UTF-8
    username: dev
    password: salfProject11@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true